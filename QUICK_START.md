# ⚡ Быстрый старт - команды по порядку

## 1️⃣ Установка зависимостей
```powershell
pip install -r requirements.txt
```

## 2️⃣ Создание бота
1. Откройте Telegram → @BotFather
2. Отправьте: `/newbot`
3. Следуйте инструкциям
4. **Скопируйте токен!**

## 3️⃣ Установка токена
```powershell
$env:BOT_TOKEN="ваш_токен_здесь"
```

## 4️⃣ Тестирование веб-приложения (локально)

### Терминал 1 - Веб-сервер:
```powershell
cd web
python -m http.server 8000
```

### Терминал 2 - ngrok:
```powershell
ngrok http 8000
```
**Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)

## 5️⃣ Обновление URL в bot.py
Откройте `bot.py`, строка 28, замените:
```python
web_app=WebAppInfo(url="https://abc123.ngrok.io/index.html")
```

## 6️⃣ Настройка в BotFather
1. Откройте @BotFather
2. `/setmenubutton`
3. Выберите бота
4. Введите текст: `Открыть приложение`
5. Введите URL: `https://abc123.ngrok.io/index.html`

## 7️⃣ Запуск бота
```powershell
python bot.py
```

## 8️⃣ Тестирование
1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🚀 Открыть приложение"

---

## 📝 Важные заметки

- **Токен** нужно устанавливать в каждом новом терминале
- **ngrok URL** меняется при перезапуске (бесплатный план)
- Для продакшена используйте постоянный хостинг
- Веб-приложение **обязательно** должно быть на HTTPS


