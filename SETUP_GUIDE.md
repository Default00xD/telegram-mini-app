# 📋 Подробная инструкция по настройке Telegram Mini App

## Шаг 1: Установка зависимостей Python

### 1.1. Установите библиотеку для работы с Telegram ботом

Откройте терминал в папке проекта и выполните:

```powershell
pip install -r requirements.txt
```

**Ожидаемый результат:** Установится библиотека `python-telegram-bot`

---

## Шаг 2: Создание Telegram бота

### 2.1. Откройте Telegram и найдите @BotFather

1. Откройте приложение Telegram
2. В поиске введите `@BotFather`
3. Нажмите на бота и начните диалог

### 2.2. Создайте нового бота

Отправьте команду:
```
/newbot
```

**BotFather спросит:**
1. **Имя бота** (например: `Мой Mini App`)
2. **Username бота** (должен заканчиваться на `bot`, например: `my_mini_app_bot`)

**Результат:** BotFather выдаст вам **токен** вида:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

⚠️ **ВАЖНО:** Сохраните этот токен! Он понадобится в следующем шаге.

---

## Шаг 3: Настройка токена бота

### 3.1. Установите переменную окружения BOT_TOKEN

**В PowerShell (ваш случай):**
```powershell
$env:BOT_TOKEN="ваш_токен_от_BotFather"
```

**Пример:**
```powershell
$env:BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
```

### 3.2. Проверьте, что токен установлен

```powershell
echo $env:BOT_TOKEN
```

Должен вывести ваш токен.

⚠️ **Примечание:** После закрытия терминала переменная исчезнет. Для постоянной установки используйте:
```powershell
[System.Environment]::SetEnvironmentVariable('BOT_TOKEN', 'ваш_токен', 'User')
```

---

## Шаг 4: Обновление URL веб-приложения в bot.py

### 4.1. Откройте файл `bot.py`

Найдите строку 28:
```python
web_app=WebAppInfo(url="https://your-domain.com/web/index.html")
```

### 4.2. Замените URL на ваш реальный адрес

**Для тестирования (локально):**
- Сначала нужно развернуть веб-приложение (см. Шаг 5)
- Используйте ngrok для создания HTTPS туннеля

**Для продакшена:**
- Используйте реальный домен с HTTPS
- Например: `https://myapp.example.com/web/index.html`

---

## Шаг 5: Развертывание веб-приложения

Telegram требует, чтобы веб-приложение было доступно по **HTTPS**. Есть несколько вариантов:

### Вариант A: Локальное тестирование с ngrok (рекомендуется для начала)

#### 5.1. Установите ngrok

1. Скачайте с https://ngrok.com/download
2. Распакуйте в папку (например, `C:\ngrok`)
3. Добавьте в PATH или используйте полный путь

#### 5.2. Запустите локальный веб-сервер

В терминале выполните:
```powershell
cd web
python -m http.server 8000
```

Оставьте это окно открытым.

#### 5.3. В новом терминале запустите ngrok

```powershell
ngrok http 8000
```

**Результат:** Вы получите HTTPS URL вида:
```
https://abc123.ngrok.io
```

#### 5.4. Обновите URL в bot.py

Замените в `bot.py` строку 28:
```python
web_app=WebAppInfo(url="https://abc123.ngrok.io/index.html")
```

⚠️ **Важно:** URL от ngrok меняется при каждом перезапуске (на бесплатном плане).

### Вариант B: Развертывание на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите папку `web` в репозиторий
3. Включите GitHub Pages в настройках репозитория
4. Используйте URL вида: `https://username.github.io/repository/index.html`

### Вариант C: Развертывание на Vercel/Netlify

1. Зарегистрируйтесь на vercel.com или netlify.com
2. Загрузите папку `web`
3. Получите бесплатный HTTPS домен

---

## Шаг 6: Настройка Web App в BotFather

### 6.1. Откройте @BotFather снова

### 6.2. Выберите вашего бота

Отправьте:
```
/mybots
```
Выберите вашего бота из списка.

### 6.3. Настройте Web App

1. Нажмите **"Bot Settings"**
2. Нажмите **"Menu Button"** или **"Web App"**
3. Выберите **"Edit Menu Button"**
4. Введите текст кнопки (например: `Открыть приложение`)
5. Введите URL вашего веб-приложения (HTTPS)

**Альтернативный способ:**
Отправьте команду:
```
/setmenubutton
```
Выберите бота, введите текст кнопки и URL.

---

## Шаг 7: Запуск бота

### 7.1. Убедитесь, что токен установлен

```powershell
echo $env:BOT_TOKEN
```

### 7.2. Запустите бота

```powershell
python bot.py
```

**Ожидаемый результат:**
```
INFO - Бот запущен...
```

Бот должен быть онлайн и готов к работе!

---

## Шаг 8: Тестирование

### 8.1. Откройте вашего бота в Telegram

Найдите бота по username (например, `@my_mini_app_bot`)

### 8.2. Отправьте команду /start

Бот должен ответить приветствием и показать кнопку "🚀 Открыть приложение"

### 8.3. Нажмите на кнопку

Должно открыться веб-приложение в Telegram

### 8.4. Протестируйте функционал

- Проверьте отображение информации о пользователе
- Введите данные в форму и отправьте
- Проверьте, что данные сохраняются в базе данных

---

## Шаг 9: Проверка базы данных

### 9.1. Проверьте создание файла БД

После первого запуска должен появиться файл `telegram_app.db`

### 9.2. Просмотр данных (опционально)

Можно использовать SQLite браузер или Python:

```python
import sqlite3
conn = sqlite3.connect('telegram_app.db')
cursor = conn.cursor()
cursor.execute('SELECT * FROM users')
print(cursor.fetchall())
conn.close()
```

---

## 🔧 Решение проблем

### Проблема: "BOT_TOKEN не установлен"

**Решение:** Убедитесь, что вы установили переменную окружения:
```powershell
$env:BOT_TOKEN="ваш_токен"
```

### Проблема: "ModuleNotFoundError: No module named 'telegram'"

**Решение:** Установите зависимости:
```powershell
pip install -r requirements.txt
```

### Проблема: Web App не открывается

**Решение:** 
- Убедитесь, что URL использует HTTPS
- Проверьте, что веб-сервер запущен и доступен
- Проверьте URL в BotFather

### Проблема: ngrok URL меняется

**Решение:** 
- Используйте бесплатный аккаунт ngrok для стабильного URL
- Или разверните на постоянном хостинге

---

## ✅ Чек-лист готовности

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан бот через @BotFather
- [ ] Получен токен бота
- [ ] Установлена переменная окружения BOT_TOKEN
- [ ] Обновлен URL веб-приложения в bot.py
- [ ] Веб-приложение развернуто и доступно по HTTPS
- [ ] Настроен Menu Button в BotFather
- [ ] Бот запущен и работает (`python bot.py`)
- [ ] Протестирована команда /start
- [ ] Протестировано открытие Web App
- [ ] Протестирована отправка данных

---

## 🚀 Готово!

После выполнения всех шагов ваше Telegram Mini App должно работать!


